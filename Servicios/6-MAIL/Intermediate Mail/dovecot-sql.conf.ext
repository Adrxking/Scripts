driver = mysql

connect = host=localhost dbname=correo user=usuariocorreo password=usuariocorreo

default_pass_scheme = MD5

password_query = SELECT email as user, password FROM virtual_users WHERE email='%u';
